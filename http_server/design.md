# 一些设计

过程采用两步分开fucntion,第一步过滤非法数据。返回中间数据，和错误代码。 如果中间数据为空，第二步就不用执行了。
第二步继续加工中间数据和错误代码。

# 一些技巧：
- 10进制数字转16进制2位str. ("0%s"%hex(num)[2:])[-2:] 

# 一些灵光一闪
怎样改变主动和被动，就像xena的题目一样。把方法feed给数据。

中间件的思想。可以在函数间不断地传递结果。


# 温湿度模块：
- 完全体：可以设置温湿度模块的个数，连接方式。 自动判断？ 根据设置来自动生成？ 或者程序小改一下就能行。
- 功能： 读取串口信息，解析信息，存入缓存，格式化输出。怎样格式化输出？参考腾讯的文档。

# 温湿度数据结构：{index1:{temp:xxx,hum:xxx},index2:{temp:xxx,hum:xxx}}.
在dataCenter.tem_hum

# 勾子： 在轮询过程中，有两个勾子：
- Updated: 是否有任何信息变化，如设备上下线 ,模块掉线，新模块上线。
    - 这个勾子可以加分析，细化到：设备上线，设备下线，啥啥的   
    + 
    + 

# 
